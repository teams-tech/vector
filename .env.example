# Public client configuration (required for voice session startup)
NEXT_PUBLIC_ELEVENLABS_AGENT_ID=

# Server-only configuration (required for /api/chat, must be absolute http(s) URL)
CHAT_API_URL=https://chat-backend.internal.example/api/chat

# Comma-separated browser Origin allowlist for /api/chat (required; include local dev explicitly)
CHAT_ALLOWED_ORIGINS=http://localhost:3000,https://mia.teams-tech.example
